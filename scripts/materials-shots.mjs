import fs from 'node:fs'; import path from 'node:path'; import { ROOT } from './materials.mjs';
const slug=process.argv[2]; if(!slug) throw Error('用法：npm run materials:shots <slug>'); const content=JSON.parse(fs.readFileSync(path.join(ROOT,'materials-src',slug,'content.json'),'utf8'));
const shots=path.join(ROOT,'materials-src',slug,'shots'); fs.mkdirSync(shots,{recursive:true});
let chromium; try { ({ chromium }=await import('playwright')); } catch { throw Error('找不到 playwright；請先 npm install -D playwright'); }
const browser=await chromium.launch({channel:'chrome'}).catch(()=>chromium.launch()); const page=await browser.newPage({viewport:{width:390,height:844}}); const errors=[]; page.on('console',m=>{if(m.type()==='error')errors.push(m.text())}); await page.goto(new URL(`file://${path.join(ROOT,'public/materials',`${slug}-${content.version}`,'index.html')}`).href); const report={slug,viewports:[],errors,nodeIds:content.nodes.map(n=>n.id),questionIds:[...content.foundation,...content.cases].map(q=>q.id)};
for(const [width,height] of [[390,844],[1280,800]]){await page.setViewportSize({width,height}); if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth)) errors.push(`${width}px 水平捲動`); await page.screenshot({path:path.join(shots,`${width}.png`),fullPage:true}); report.viewports.push({width,height});}
for(const state of content.lab.states){await page.locator(`button[data-state="${state.id}"]`).click();await page.screenshot({path:path.join(shots,`lab-${state.id}.png`),fullPage:true})} for(const n of content.nodes){await page.locator(`[data-node="${n.id}"]`).click();await page.screenshot({path:path.join(shots,`node-${n.id}.png`),fullPage:true})}
report.ok=!errors.length; fs.writeFileSync(path.join(shots,'report.json'),JSON.stringify(report,null,2)); await browser.close(); console.log(JSON.stringify(report)); if(errors.length)process.exitCode=1;
