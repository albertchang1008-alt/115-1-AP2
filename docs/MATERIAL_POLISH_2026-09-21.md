# 交給 Codex：三份心臟／紅血球教材改善（2026-09-21，教師發包，Claude 撰寫）

對象檔案（皆在 `html/`，尚未匯入 `public/materials/`）：
`心臟構造.html`、`心臟血液供應.html`、`紅血球的恆定機制.html`

在 `feature/1.4.0-unit-model` 上接續提交；完成後停下，**不 push、不部署、不匯入教材工作室**，於 `handoff.md` 標示「教材改善待 Claude 驗收」。

## 不可變動的部分

- 探索節點 ID（`<slug>-node-01..06`）、題目 ID（`<slug>-foundation-q01..06`、`<slug>-case-q01..05`）、診斷分母 6 節點／11 題。
- 首次作答才是診斷依據：`CourseLearning.answer` 每次點選照送，不可改成只送最後一次，也不可在重試時用新 ID。
- 預測題與滑桿仍不送任何診斷事件。
- 通關條件：第一關逐題答對、第二關 5 題全對才 `complete()`。

## 任務 1（直接實作）：紅血球「恆定全流程」圖手機版改直式

現況 `.rbc-overview svg { min-width:720px }`，360px 手機需在框內左右滑，右半段（紅血球循環→網狀內皮系統、回收與膽紅素）容易被忽略。

要求：≤760px 時改為**直式由上而下**的流程（可另畫一張直式 SVG，或改為 HTML 卡片＋向下箭頭），內容與負回饋箭頭（綠色）完整保留，文字不小於 13px，不得出現水平捲動。桌機維持現有橫式圖。

## 任務 2（直接實作）：第一關選項洗牌

第二關已洗牌，第一關沒有。三份教材的第一關改為每次進入該題時洗牌選項（沿用既有 `shuffleArray`，以 `isCorrect` 判斷而非原始索引），避免學生記位置。題目順序維持固定（先備知識有鋪陳順序）。

## 任務 3（**先提方案，教師核定後才實作**）：第二關重試的鑑別度

現況：答錯立即顯示正解＋解析，重試是同樣 5 題（只換順序），學生靠記憶即可通關。首次診斷不受影響，但「通關」幾乎不具鑑別度。

請提出 2–3 個方案並列出利弊，至少考慮：
- A. 答錯只顯示解析方向（hint），不標示正確選項；全部作答完再看完整解析。
- B. 每份教材把病例題擴充為題組（例如 10 題），每輪抽 5 題；**注意**：新增題目會改變診斷分母與題目 ID 清單，需說明對 `questionTotal`、`shared/materials.ts` 與既有診斷資料的影響。
- C. 重試前需先回到對應探索節點閱讀（冷卻或提示），再開放第二關。

在 `handoff.md` 寫下方案與建議，**不要實作任務 3**，等待教師選擇。

## 驗證

- 內嵌 JS 語法檢查；`npm test`（`tests/material.test.ts` 補上：第一關選項洗牌、紅血球窄版直式流程存在且無 `min-width:720px` 作用於窄版）；`npm run build`；`git diff --check`。
- 以瀏覽器 390px 與桌機寬度各看一次三份教材；若無法預覽，於 handoff 註明。
