# 教材元件庫設計規範

教材元件庫 v1 使用 `--oxy`／`--deoxy` 表示含氧與缺氧血，`--accent` 僅供互動強調；底色、文字及狀態色在 `materials-src/kit/kit.css` 定義，採深色文字搭淺底以維持至少 4.5:1 對比。正文 16px，窄版改單欄且不容許水平捲動。

圖示與教學圖皆以內嵌 SVG 呈現；SVG 文字保留 `<text>`，方便檢查。互動按鈕提供原生鍵盤操作與 ARIA 狀態；減少動態偏好會停用動畫。
